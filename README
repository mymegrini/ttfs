Tiny Toy File System project description

bin: contains executables (contains .gitignore)
doc: contains documentation
dev: contains TTFS disks (contains .gitignore)
lib: contains library source files
obj: contains compiler-generated object files (contains .gitignore)
src: contains source files

Makefile:
	make command instruction tree
setup.sh:
	launches 'make' command
	adds '$PWD/bin' folder to PATH environment variable
	adds '$PWD/lib' folder to DYNAMIC LIBRARY environment variables

INSTALLATION GUIDE
	> execute shell command 'source setup.sh'

Authors :
	Victor		Fontaine
	Mohammed Younes Megrini
	Nicolas  	Scotto Di Perto